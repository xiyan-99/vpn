# App Store 更新检测 - 配置说明

## 📝 如何自定义应用包名和检查间隔

由于Surge模块的 `#!argument` 语法在某些版本中可能不支持，本模块采用**直接修改模块文件**的方式来自定义参数。

## 方法一：下载并修改本地模块（推荐）

### 1. 下载模块文件

下载 `appstore.sgmodule` 文件到本地（Surge配置目录）。

### 2. 修改应用包名

打开 `appstore.sgmodule` 文件，找到以下行：

```ini
[Script]
appstore_panel = type=generic,timeout=10,script-path=https://raw.githubusercontent.com/xiyan-99/vpn/refs/heads/main/appstore.js,argument=applist=com.liguangming.Shadowrocket|com.nssurge.inc.surge-ios|com.ruikq.decar
```

**修改 `argument=applist=` 后面的内容**，填入你想监控的应用包名，用 `|` 分隔：

```ini
argument=applist=你的包名1|你的包名2|你的包名3
```

**示例：**

监控 Shadowrocket、Telegram、微信：
```ini
argument=applist=com.liguangming.Shadowrocket|ph.telegra.Telegraph|com.tencent.xin
```

### 3. 修改检查间隔

找到以下行：

```ini
[Panel]
appstore_panel = script-name=appstore_panel,update-interval=300
```

**修改 `update-interval=` 后面的数字**（单位：秒）：

```ini
update-interval=600  # 改为600秒（10分钟）
```

**推荐值：**
- 频繁检查：60-180秒（1-3分钟）
- 正常使用：300-600秒（5-10分钟）
- 省流量：1800-3600秒（30分钟-1小时）

### 4. 在Surge中加载本地模块

1. 将修改好的 `appstore.sgmodule` 文件放到 Surge 配置目录
2. 在 Surge 中选择 "模块" → "本地模块"
3. 选择你修改的 `appstore.sgmodule` 文件

## 方法二：使用在线模块并手动配置

如果你想使用在线模块但需要自定义参数，可以：

### 1. 安装在线模块

```
https://raw.githubusercontent.com/xiyan-99/vpn/refs/heads/main/appstore.sgmodule
```

### 2. 在配置文件中覆盖

在你的主配置文件中添加：

```ini
[Panel]
appstore_panel = script-name=appstore_custom,update-interval=你的间隔秒数

[Script]
appstore_custom = type=generic,timeout=10,script-path=https://raw.githubusercontent.com/xiyan-99/vpn/refs/heads/main/appstore.js,argument=applist=你的包名1|你的包名2|你的包名3
```

这会覆盖模块中的默认配置。

## 📱 支持的分隔符

脚本支持以下三种分隔符来分隔应用包名：

1. **竖线** `|` （推荐，避免与参数分隔符冲突）
   ```
   argument=applist=com.app1|com.app2|com.app3
   ```

2. **逗号** `,`
   ```
   argument=applist=com.app1,com.app2,com.app3
   ```

3. **分号** `;`
   ```
   argument=applist=com.app1;com.app2;com.app3
   ```

## 📖 完整配置示例

### 示例1：监控代理工具（每5分钟检查）

```ini
#!name=appstore自动检测更新
#!desc=appstore自动检测更新面板
#!author=夕颜 wx: 1418581664
#!icon=https://img.iosxy.xin/xiyan.png
#!system=ios
#!homepage=https://iosxy.xin

[Panel]
appstore_panel = script-name=appstore_panel,update-interval=300

[Script]
appstore_panel = type=generic,timeout=10,script-path=https://raw.githubusercontent.com/xiyan-99/vpn/refs/heads/main/appstore.js,argument=applist=com.liguangming.Shadowrocket|com.nssurge.inc.surge-ios|com.ruikq.decar
```

### 示例2：监控社交应用（每10分钟检查）

```ini
[Panel]
appstore_panel = script-name=appstore_panel,update-interval=600

[Script]
appstore_panel = type=generic,timeout=10,script-path=https://raw.githubusercontent.com/xiyan-99/vpn/refs/heads/main/appstore.js,argument=applist=ph.telegra.Telegraph|com.tencent.xin|com.tencent.mqq
```

### 示例3：监控单个应用（每1分钟检查）

```ini
[Panel]
appstore_panel = script-name=appstore_panel,update-interval=60

[Script]
appstore_panel = type=generic,timeout=10,script-path=https://raw.githubusercontent.com/xiyan-99/vpn/refs/heads/main/appstore.js,argument=applist=com.liguangming.Shadowrocket
```

## 🔍 如何获取应用包名

### 方法1：使用在线工具
访问：https://tools.lancely.tech/apple/app-info
输入应用名称即可查询包名

### 方法2：iTunes API
浏览器访问：
```
https://itunes.apple.com/search?term=应用名&entity=software&country=cn
```
在返回的JSON中找到 `bundleId` 字段

### 方法3：通过App Store链接
如果你有App Store链接（如 `https://apps.apple.com/app/id123456789`），
浏览器访问：
```
https://itunes.apple.com/lookup?id=123456789&country=cn
```
在返回的JSON中找到 `bundleId` 字段

## 📋 常见应用包名参考

### 代理工具
```
com.liguangming.Shadowrocket   # Shadowrocket
com.nssurge.inc.surge-ios       # Surge
com.ruikq.decar                 # Loon
```

### 社交通讯
```
ph.telegra.Telegraph            # Telegram
com.tencent.xin                 # 微信
com.tencent.mqq                 # QQ
```

### 常用工具
```
com.zhihu.ios                   # 知乎
com.ss.iphone.ugc.Aweme         # 抖音
```

## ⚠️ 注意事项

1. **面板只在策略选择视图时自动更新**
   - 如果设置 `update-interval=1`，每次切到策略视图都会更新
   - 如果设置过大的间隔，可能需要手动刷新

2. **不要添加太多应用**
   - 建议监控3-10个应用
   - 应用太多会增加查询时间和流量消耗

3. **某些应用可能查询失败**
   - 应用未在当前区域上架
   - 应用已下架
   - 包名错误
   - API限流

4. **修改后需要重新加载模块**
   - 修改模块文件后，在Surge中禁用再启用模块
   - 或者重启Surge

## 🎯 快速开始

**最简单的方法：**

1. 复制 `appstore.sgmodule` 文件内容
2. 修改其中的 `argument=applist=` 为你想监控的应用包名
3. 修改 `update-interval=` 为你想要的检查间隔
4. 保存为本地文件，在Surge中加载

**完成！** 🎉

